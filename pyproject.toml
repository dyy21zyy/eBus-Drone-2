[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ebus_drone_rl"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["numpy","pandas","PyYAML","matplotlib","torch","scipy"]

[tool.pytest.ini_options]
pythonpath = ["."]
